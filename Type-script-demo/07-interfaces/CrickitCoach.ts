import { Coach } from "./Coach";

export class CrickitCoach implements Coach {
    getDailyWorkOut(): string {
        return "This is my crickt coash drink enough water to prevent from dehidration";
    }

}